name: eu_dev_compensation
version: "1.0.0"
config-version: 2

profile: eu_dev_compensation

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - target
  - dbt_packages

seeds:
  eu_dev_compensation:
    +schema: raw

models:
  eu_dev_compensation:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
      fact_compensation:
        +materialized: incremental
